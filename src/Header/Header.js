import React from 'react';
import './header.scss';

export const Header = () => {
    return (
        <React.Fragment>
            <a href="#" className="center">LOGO</a>
        </React.Fragment>
    )
}